import 'package:flutter/material.dart';

class Settings {
  static const largeFontSize = 16.0;
  static const windowMinHeight = 600.0;
  static const windowMinWidth = 800.0;

  static const windowMinSize = Size(windowMinWidth, windowMinHeight);
}
